# Ledger — Frontend

React + Vite + Tailwind CSS client for the Ledger e-learning platform.

See the project-level README at `../README.md` for full setup instructions, architecture notes, and how the mock-data/live-API switch works.

## Quick Start
```bash
npm install
npm run dev
```
Runs at http://localhost:5173
